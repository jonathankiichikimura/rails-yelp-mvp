<%= simple_form_for @restaurant, html: { class: "container mt-4" } do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <%= f.submit "Submit Restaurant", class: "btn btn-primary" %>
<% end %>
