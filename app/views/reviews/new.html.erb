<%= simple_form_for [@restaurant, @review], html: { class: "container mt-4" } do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit "Submit Review", class: "btn btn-primary" %>
<% end %>
